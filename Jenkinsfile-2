pipeline {
    agent any
    stages {
        stage('Build docker container') {
            steps {
                sh 'docker run python:3.12.0-alpine3.18'
            }
        }

        stage('Test'){
            steps {
                sh 'python3 --version'
            }
        }
    }
}